# Go Auth Client

> Work in progress; though it's 90% complete.

A simple example of a Flutter app with signup and login.

It's designed to accompany a simple API built with Go (to follow), though it doesn't really matter what the backend is implemented in.

The only thing to note is that it expects API responses to be in a particular format; more details to follow.

## Usage

Usage instructions, documentation and a link to a blog post to follow.